<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.order(:name.downcase), prompt: "Choose an ingredient" %>
  <%= f.submit "Submit dose", class: "btn btn-primary" %>
<% end %>
